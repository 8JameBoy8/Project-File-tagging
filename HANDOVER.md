# Archive App - เอกสารส่งมอบงาน (Handover Document)

เอกสารฉบับนี้จัดทำขึ้นเพื่อส่งต่องานให้กับผู้พัฒนาท่านอื่น (Developer Handover) โดยจะรวบรวมสถานะปัจจุบันของระบบ สิ่งที่ได้ทำไปแล้ว วิธีการรันโปรเจกต์ และสิ่งที่ต้องนำไปต่อยอดครับ

---

## 🏗️ 1. ภาพรวมของโปรเจกต์ (Project Overview)
โปรเจกต์นี้คือ **ระบบจัดการไฟล์ (File Archive Application)** ที่สามารถอัปโหลดไฟล์ และจัดการไฟล์ผ่านระบบ **แท็ก (Tags)** ได้ โครงสร้างปัจจุบันเป็นแบบ Full-stack อยู่ในที่เดียว (Monorepo)

**Tech Stack ปัจจุบัน:**
- **Framework:** Next.js (App Router) + TypeScript
- **Database:** SQLite (เก็บไฟล์ใน local `dev.db`)
- **ORM:** Prisma
- **Styling:** CSS ธรรมดา (`globals.css`) โดยยึดดีไซน์มาจากไฟล์ HTML เดิมที่คุณออกแบบไว้
- **File Storage:** ปัจจุบันเก็บไฟล์อัปโหลดไว้ในโฟลเดอร์ `/uploads` ภายในเครื่อง (Local Storage)

---

## ✅ 2. สิ่งที่พัฒนาเสร็จไปแล้ว (Completed Features)

### 2.1 โครงสร้างฐานข้อมูล (Database Schema)
มีการสร้างตารางด้วย Prisma (`prisma/schema.prisma`) เสร็จสมบูรณ์:
- **`Tag`**: เก็บชื่อแท็ก และสีของแท็ก
- **`File`**: เก็บชื่อไฟล์, ประเภทไฟล์ (image, video, etc.), นามสกุลไฟล์, ขนาดไฟล์, และ path ที่ชี้ไปยังที่เก็บไฟล์
- **`FileTag`**: ตารางเชื่อมความสัมพันธ์แบบ Many-to-Many ระหว่างไฟล์และแท็ก

### 2.2 APIs (Backend Routes)
API ทั้งหมดถูกสร้างด้วย Next.js Route Handlers (`src/app/api/...`) คืนค่าเป็น JSON และใช้งานได้จริงประกอบด้วย:
- `/api/tags`:
  - `GET`: ดึงแท็กทั้งหมด
  - `POST`: สร้างแท็กใหม่
  - `PUT /api/tags/[id]`: แก้ไขแท็ก (ชื่อ/สี)
  - `DELETE /api/tags/[id]`: ลบแท็ก
- `/api/files`:
  - `GET`: ดึงรายการไฟล์ (รองรับการ query เช่น `?sort=desc` และ `?tagIds=...` เพื่อฟิลเตอร์)
  - `POST`: อัปโหลดไฟล์ (รับข้อมูลแบบ `multipart/form-data`) พร้อมสร้างความสัมพันธ์กับแท็ก
  - `GET /api/files/[id]`: ดึงข้อมูลไฟล์ตาม ID
  - `DELETE /api/files/[id]`: ลบไฟล์ (ตัว API จะเข้าไปลบไฟล์จริงในโฟลเดอร์ `/uploads` พร้อมลบใน DB ด้วย)
  - `GET /api/files/[id]/serve`: อ่านและเสิร์ฟไฟล์ตรงๆ (สำหรับแสดงรูป หรือดูวิดีโอ/ฟังเพลงบนเว็บ)
  - `GET /api/files/[id]/download`: บังคับให้เบราว์เซอร์ดาวน์โหลดไฟล์
  - `PUT /api/files/[id]/tags`: ผูก/ถอดแท็กของไฟล์นั้นๆ

### 2.3 หน้าเว็บ (Frontend UI)
แปลงหน้า HTML เดิม ให้กลายเป็น React Components และใช้ Client Components (`"use client"`) เพื่อจัดการ State แล้ว:
- `หน้าหลัก (/)`: ดูรายการไฟล์ทั้งหมด, ฟิลเตอร์ตามแท็ก, เรียงลำดับโหมดต่างๆ, อัปโหลดไฟล์ใหม่, พรีวิวและดาวน์โหลดไฟล์
- `หน้าสร้างแท็ก (/create-tag)`: สร้างแท็กใหม่
- `หน้าจัดการแท็ก (/manage-tag)`: แก้ไขหรือลบแท็กที่มีอยู่ พร้อมแสดงว่าแต่ละแท็กถูกผูกอยู่กับไฟล์อะไรบ้าง (แสดงเป็น badges)

---

## 🚀 3. วิธีการรันโปรเจกต์ (สำหรับเริ่มทำต่อ)
ให้เพื่อนที่รับช่วงต่อทำตามขั้นตอนดังนี้เพื่อรันระบบในเครื่องตัวเอง:

1. **ติดตั้ง Dependencies:**
   ```bash
   npm install
   ```

2. **เตรียมฐานข้อมูล โครงสร้าง Schema:**
   *(โปรเจกต์นี้ใช้ SQLite ซึ่งสร้างฐานข้อมูลจำลองไว้แล้วที่ prisma/dev.db แต่ถ้ารันครั้งแรกควร Push Schema ก่อน)*
   ```bash
   npx prisma generate
   npx prisma db push
   ```

3. **เตรียมโฟลเดอร์อัปโหลด:**
   ให้สร้างโฟลเดอร์ชื่อ `uploads` ไว้ที่ระดับนอกสุดของโปรเจกต์ (โฟลเดอร์เดียวกับ `package.json`)
   *(อย่าลืมว่าไฟล์ใน /uploads จะไม่ถูก push ขึ้น Git เพราะถูกใส่ไว้ใน .gitignore แล้ว)*

4. **รันเซิร์ฟเวอร์:**
   ```bash
   npm run dev
   ```
   จากนั้นเข้าไปที่ `http://localhost:3000`

---

## 🛠️ 4. สิ่งที่ต้องทำต่อ / ยังขาดอยู่ (Next Steps)

เพื่อนที่จะนำไปทำต่อ ควรพิจารณาทำสิ่งเหล่านี้เพื่อความสมบูรณ์ของระบบก่อนขึ้น Production:

1. **ระบบล็อกอิน (Authentication / Authorization) ⚠️สำคัญมาก**
   - **ปัญหาปัจจุบัน:** ตอนนี้ทุกคนที่เข้าถึง URL ได้ สามารถลบ อัปโหลด หรือเปลี่ยนแท็กได้อย่างอิสระ
   - **แนวทางแก้:** ควรเชื่อมระบบ Auth (เช่น **NextAuth.js** หรือระบบ Login ที่เพื่อนใช้อยู่)
   - *คำแนะนำ:* เมื่อทำ Auth แล้ว อาจจะต้องเข้าไปแก้ Schema เพิ่มคอลัมน์ `userId` ในตาราง `File` และ `Tag` เพื่อให้รู้ว่าใครเป็นเจ้าของไฟล์นั้นๆ และ API ควรตรวจเช็ค Session เสมอก่อนให้ลบ/อัปโหลดไฟล์

2. **ระบบเก็บไฟล์บน Production (Cloud Storage)**
   - **ปัญหาปัจจุบัน:** ไฟล์ที่อัปโหลดถูกเก็บไว้ในโฟลเดอร์ `/uploads` ภายในโปรเจกต์ (Local File System) 
   - **ทำไมต้องแก้:** หากนำโปรเจกต์นี้ไป Deploy บน Cloud Provider ยอดฮิตอย่าง **Vercel** หรือ **Render** ไฟล์ต่างๆ จะหายไปทุกครั้งที่มีการปล่อยโค้ดใหม่ (Ephemeral File System)
   - **แนวทางแก้:** เปลี่ยนตัวจัดการจุดอัปโหลดไฟล์ใน `/api/files/route.ts` ให้ไปส่งไฟล์อัปโหลดเข้าบริการด้าน Storage ภายนอกแทน เช่น **AWS S3, Cloudinary หรือ Vercel Blob** และให้บันทึก URL ตรงส่งกลับมาเก็บใน DB.

3. **การจัดการข้อผิดพลาด & Validate Data (Error Handling)**
   - ตอนนี้ API และ Frontend ยัง Validate ข้อมูลพื้นฐานอยู่ ควรนำไลบรารีอย่าง **Zod** เข้ามาครอบเพื่อให้การรับ Parameter และ Form Data ปลอดภัย (Type Safe) มากยิ่งขึ้น

4. **ฟังก์ชันที่สามารถพัฒนาเพิ่มได้ (Optional)**
   - **ระบบค้นหาด้วยชื่อไฟล์ (Search Bar):** ปัจจุบันมีโค้ด UI รองรับในหน้าแรก แต่อาจต้องไปอัปเดต API `/api/files` ให้รับ parameter `?search=` แล้ว query จากฐานข้อมูลรูปแบบ `contains` (LIKE) เพิ่มเข้าไป
   - **Loading State ตอนอัปโหลด:** ตอนอัปโหลดไฟล์ขนาดใหญ่ อาจจะทำ Progress Bar % เพื่อให้ User ทราบว่ากำลังทำงาน

---

ถ้ามีข้อสงสัย หรืออยากให้ปรับแก้ไขในส่วนที่พูดถึงด้านบนก่อนที่จะส่งต่อ สามารถพิมพ์บอกผม (AI) ตัวนี้ต่อได้เลยครับ พร้อมช่วยงานเต็มที่!
