// src/__tests__/integration/constants.ts — ค่าที่ทุกไฟล์ integration test ใช้ร่วมกัน
export const PORT = 3100
export const BASE_URL = `http://localhost:${PORT}`
export const TEST_DB_FILE = 'test-integration.db'
export const TEST_DB_URL = `file:./${TEST_DB_FILE}`
